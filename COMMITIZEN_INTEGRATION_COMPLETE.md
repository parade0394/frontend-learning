# ✅ Git 工作流自动化完整指南

## 📖 目录

- [项目概述](#项目概述)
- [工具链架构](#工具链架构)
- [完整工作流程](#完整工作流程)
- [配置文件详解](#配置文件详解)
  - [1. package.json - 核心配置中心](#1-packagejson---核心配置中心)
  - [2. .husky/pre-commit - 提交前钩子](#2-huskypre-commit---提交前钩子)
  - [3. .husky/commit-msg - 提交信息验证钩子](#3-huskycommit-msg---提交信息验证钩子)
  - [4. commitlint.config.js - 提交信息验证规则](#4-commitlintconfigjs---提交信息验证规则)
  - [5. .cz-config.cjs - Commitizen 交互式配置](#5-cz-configcjs---commitizen-交互式配置)
- [完整提交流程实例](#完整提交流程实例)
- [工具协同工作原理](#工具协同工作原理)
- [常见问题与解决方案](#常见问题与解决方案)
- [最佳实践](#最佳实践)
- [配置文件关系图](#配置文件关系图)
- [相关资源](#相关资源)
- [项目统计](#项目统计)
- [快速命令参考](#快速命令参考)
- [总结](#总结)

---

## 🎉 项目概述

本项目集成了完整的 Git 工作流自动化系统，通过多个工具的协同工作，实现了代码质量自动检查和提交信息规范化。

## 📦 工具链架构

| 工具                | 版本    | 作用            | 触发时机          |
| ------------------- | ------- | --------------- | ----------------- |
| **Husky**           | 9.1.7   | Git 钩子管理    | Git 操作时        |
| **lint-staged**     | 16.2.6  | 暂存文件检查    | pre-commit 钩子   |
| **commitlint**      | 20.1.0  | 提交信息验证    | commit-msg 钩子   |
| **Commitizen**      | 4.3.1   | 交互式提交工具  | 手动运行          |
| **cz-customizable** | 7.5.1   | 自定义配置      | Commitizen 使用时 |
| **Prettier**        | 3.6.2   | 代码格式化      | lint-staged 调用  |
| **ESLint**          | 9.39.1  | JavaScript 检查 | lint-staged 调用  |
| **Stylelint**       | 16.25.0 | CSS 检查        | lint-staged 调用  |

## 🔄 完整工作流程

### 流程图

```text
开发者修改代码
    ↓
git add .  (添加到暂存区)
    ↓
npm run commit  (推荐) 或 git commit -m "..."
    ↓
┌─────────────────────────────────────┐
│  阶段 1: pre-commit 钩子触发         │
│  ↓                                  │
│  Husky 执行 .husky/pre-commit       │
│  ↓                                  │
│  运行: npx lint-staged              │
│  ↓                                  │
│  根据 package.json 中的配置:        │
│  - *.{html,css,js,json,md}          │
│    → prettier --write (格式化)      │
│  - *.{js,html}                      │
│    → eslint --fix (修复 JS 问题)    │
│  - *.css                            │
│    → stylelint --fix (修复 CSS)     │
│  ↓                                  │
│  ✅ 所有检查通过 → 继续              │
│  ❌ 有错误 → 中止提交，显示错误      │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  阶段 2: commit-msg 钩子触发         │
│  ↓                                  │
│  Husky 执行 .husky/commit-msg       │
│  ↓                                  │
│  运行: npx commitlint --edit $1     │
│  ↓                                  │
│  读取 commitlint.config.js 配置     │
│  ↓                                  │
│  验证提交信息格式:                   │
│  - 检查 type 是否在允许列表中        │
│  - 检查格式是否符合规范              │
│  - 检查 subject 是否为空             │
│  ↓                                  │
│  ✅ 格式正确 → 提交成功              │
│  ❌ 格式错误 → 中止提交，显示错误    │
└─────────────────────────────────────┘
    ↓
提交成功，生成 commit
    ↓
git push (推送到远程)
```

### 使用 Commitizen 的流程

```text
开发者修改代码
    ↓
git add .
    ↓
npm run commit  (启动 Commitizen)
    ↓
┌─────────────────────────────────────┐
│  Commitizen 交互式界面               │
│  ↓                                  │
│  读取 .cz-config.cjs 配置            │
│  ↓                                  │
│  显示提示:                          │
│  1. 选择提交类型 (types)            │
│     → feat, fix, docs, style...     │
│  ↓                                  │
│  2. 选择影响范围 (scopes, 可选)     │
│     → animation, grid, theme...     │
│  ↓                                  │
│  3. 填写简短描述 (subject)          │
│     → 用户输入描述                  │
│  ↓                                  │
│  4. 确认提交                        │
│     → 用户确认 Y/n                  │
│  ↓                                  │
│  生成规范的 commit message:         │
│  <type>(<scope>): <subject>         │
└─────────────────────────────────────┘
    ↓
自动执行 git commit -m "生成的消息"
    ↓
触发 pre-commit 和 commit-msg 钩子
    ↓
提交成功
```

## 📁 配置文件详解

### 1. package.json - 核心配置中心

**位置**: `package.json`

**作用**: 定义所有工具的脚本命令和配置

#### scripts 配置

```json
{
  "scripts": {
    "commit": "cz", // 启动 Commitizen 交互式提交
    "prepare": "husky", // npm install 后自动安装 Git 钩子
    "lint:fix": "...", // 修复所有代码问题
    "format": "..." // 格式化所有代码
  }
}
```

**执行时机**:

- `prepare`: 在 `npm install` 后自动运行，安装 Git 钩子
- `commit`: 手动运行 `npm run commit` 时执行

#### lint-staged 配置

```json
{
  "lint-staged": {
    "*.{html,css,js,json,md}": ["prettier --write"],
    "*.{js,html}": ["eslint --fix"],
    "*.css": ["stylelint --fix"]
  }
}
```

**工作原理**:

1. 只处理 `git add` 添加到暂存区的文件
2. 按文件类型匹配不同的处理规则
3. 依次执行数组中的命令
4. 如果任何命令失败，中止提交

**示例**:

```bash
# 假设你修改了 index.html 和 styles.css
git add index.html styles.css

# pre-commit 钩子触发时:
# 1. index.html 匹配 *.{html,css,js,json,md}
#    → 执行: prettier --write index.html
# 2. index.html 匹配 *.{js,html}
#    → 执行: eslint --fix index.html
# 3. styles.css 匹配 *.{html,css,js,json,md}
#    → 执行: prettier --write styles.css
# 4. styles.css 匹配 *.css
#    → 执行: stylelint --fix styles.css
```

#### Commitizen 配置

```json
{
  "config": {
    "commitizen": {
      "path": "cz-customizable" // 使用自定义适配器
    },
    "cz-customizable": {
      "config": ".cz-config.js" // 指定配置文件路径
    }
  }
}
```

**工作原理**:

- 运行 `npm run commit` 时，Commitizen 读取这个配置
- 加载 `cz-customizable` 适配器
- 适配器读取 `.cz-config.js` 获取自定义配置

---

### 2. .husky/pre-commit - 提交前钩子

**位置**: `.husky/pre-commit`

**内容**:

```bash
npx lint-staged
```

**触发时机**: 执行 `git commit` 时，在创建 commit 之前

**执行流程**:

1. Git 检测到 commit 操作
2. 查找 `.husky/pre-commit` 文件
3. 执行文件中的命令: `npx lint-staged`
4. lint-staged 根据 `package.json` 中的配置处理暂存文件
5. 所有命令成功 → 继续提交
6. 任何命令失败 → 中止提交，显示错误

**实际例子**:

```bash
# 你修改了一个格式错误的 JS 文件
echo "const x=1;const y=2;" > test.js
git add test.js
git commit -m "test: 测试"

# pre-commit 钩子执行:
# 1. npx lint-staged 启动
# 2. 检测到 test.js 在暂存区
# 3. 执行 prettier --write test.js (格式化)
# 4. 执行 eslint --fix test.js (修复问题)
# 5. 文件被自动修复并重新添加到暂存区
# 6. 提交继续进行
```

---

### 3. .husky/commit-msg - 提交信息验证钩子

**位置**: `.husky/commit-msg`

**内容**:

```bash
npx --no -- commitlint --edit $1
```

**触发时机**: 执行 `git commit` 时，在 pre-commit 之后

**参数说明**:

- `--no`: 禁用 npm 的自动安装提示
- `--edit $1`: `$1` 是 Git 传递的临时文件路径，包含提交信息

**执行流程**:

1. pre-commit 钩子执行完成
2. Git 创建临时文件保存提交信息
3. 执行 `.husky/commit-msg` 脚本
4. commitlint 读取临时文件中的提交信息
5. 根据 `commitlint.config.js` 验证格式
6. 验证通过 → 创建 commit
7. 验证失败 → 中止提交，显示错误

**实际例子**:

```bash
# 错误的提交信息
git commit -m "修复了一个 bug"

# commit-msg 钩子执行:
# 1. commitlint 读取提交信息: "修复了一个 bug"
# 2. 检查格式: 缺少 type
# 3. 验证失败，输出错误:
#    ✖ subject may not be empty [subject-empty]
#    ✖ type may not be empty [type-empty]
# 4. 提交被中止

# 正确的提交信息
git commit -m "fix: 修复移动端布局问题"

# commit-msg 钩子执行:
# 1. commitlint 读取: "fix: 修复移动端布局问题"
# 2. 检查 type: "fix" ✓ (在允许列表中)
# 3. 检查格式: "<type>: <subject>" ✓
# 4. 验证通过
# 5. 提交成功
```

---

### 4. commitlint.config.js - 提交信息验证规则

**位置**: `commitlint.config.js`

**完整配置**:

```javascript
export default {
  // 继承约定式提交规范
  extends: ['@commitlint/config-conventional'],

  // 自定义规则
  rules: {
    // 定义允许的提交类型
    'type-enum': [
      2, // 错误级别: 2=error, 1=warning, 0=disabled
      'always', // 何时应用: always=总是, never=从不
      [
        'feat', // 新功能
        'fix', // 修复 bug
        'docs', // 文档更新
        'style', // 代码格式
        'refactor', // 重构
        'perf', // 性能优化
        'test', // 测试
        'build', // 构建系统
        'ci', // CI 配置
        'chore', // 其他变动
        'revert', // 回滚
      ],
    ],
    // 不限制 subject 大小写
    'subject-case': [0],
  },
};
```

**规则说明**:

#### type-enum 规则

- **级别 2 (error)**: 如果 type 不在列表中，提交失败
- **always**: 总是检查
- **允许的类型**: 只有列表中的 11 种类型可以使用

#### subject-case 规则

- **级别 0 (disabled)**: 不检查 subject 的大小写
- 默认情况下，约定式提交要求 subject 小写，这里禁用了该规则

**验证示例**:

```bash
# ✅ 通过验证
git commit -m "feat: 添加新功能"
git commit -m "fix(theme): 修复暗色模式"
git commit -m "docs: 更新 README"

# ❌ 验证失败
git commit -m "feature: 添加新功能"  # type 错误，应该是 feat
git commit -m "修复 bug"             # 缺少 type
git commit -m "fix"                  # 缺少 subject
git commit -m "fix:添加功能"         # 缺少空格
```

**内置规则** (继承自 @commitlint/config-conventional):

- `type-empty`: type 不能为空
- `subject-empty`: subject 不能为空
- `type-case`: type 必须小写
- `subject-full-stop`: subject 不能以句号结尾
- 更多规则见 [commitlint 文档](https://commitlint.js.org/)

---

### 5. .cz-config.js - Commitizen 交互式配置

**位置**: `.cz-config.js`

**完整配置解析**:

```javascript
export default {
  // 1. 提交类型配置
  types: [
    {
      value: 'feat', // 实际写入的值
      name: 'feat:     ✨ 新功能', // 显示给用户的文本
    },
    { value: 'fix', name: 'fix:      🐛 修复 Bug' },
    // ... 其他类型
  ],

  // 2. 影响范围配置
  scopes: [
    { name: 'animation' }, // 动画模块
    { name: 'grid' }, // Grid 布局
    { name: 'box-shadow' }, // 阴影工具
    { name: 'theme' }, // 主题系统
    { name: 'shared' }, // 公共资源
    { name: 'docs' }, // 文档
    { name: 'config' }, // 配置文件
    { name: 'deps' }, // 依赖更新
  ],

  // 3. 允许自定义 scope
  allowCustomScopes: true,

  // 4. 允许 Breaking Changes 的类型
  allowBreakingChanges: ['feat', 'fix'],

  // 5. 跳过的问题
  skipQuestions: ['body', 'footer'],

  // 6. 提示信息（中文）
  messages: {
    type: '选择你要提交的类型：',
    scope: '选择一个 scope（可选）：',
    customScope: '请输入自定义的 scope：',
    subject: '填写简短精炼的变更描述：\n',
    body: '填写更加详细的变更描述（可选）。使用 "|" 换行：\n',
    breaking: '列举非兼容性重大的变更（可选）：\n',
    footer: '列举出所有变更的 ISSUES CLOSED（可选）。例如：#31, #34：\n',
    confirmCommit: '确认提交？',
  },

  // 7. subject 长度限制
  subjectLimit: 100,
};
```

**配置项详解**:

#### types - 提交类型

- **value**: 实际写入 commit message 的值，必须与 `commitlint.config.js` 中的 `type-enum` 一致
- **name**: 在交互界面显示的文本，可以包含 Emoji 和说明

#### scopes - 影响范围

- 定义项目中的模块或功能区域
- 用户可以从列表中选择，也可以自定义（如果 `allowCustomScopes: true`）

#### allowCustomScopes

- `true`: 用户可以输入自定义 scope
- `false`: 只能从预定义列表中选择

#### skipQuestions

- 跳过不需要的问题
- 本项目跳过了 `body` 和 `footer`，简化提交流程

#### messages

- 自定义所有提示信息
- 支持中文，提高用户体验

#### subjectLimit

- 限制 subject 的最大长度
- 防止提交信息过长

**交互流程示例**:

```bash
$ npm run commit

# 步骤 1: 选择类型
? 选择你要提交的类型： (Use arrow keys)
❯ feat:     ✨ 新功能
  fix:      🐛 修复 Bug
  docs:     📝 文档更新
  style:    💄 代码格式（不影响功能）
  refactor: ♻️  重构（既不是新增功能，也不是修复 Bug）
  perf:     ⚡️ 性能优化
  test:     ✅ 测试相关
  (Move up and down to reveal more choices)

# 用户选择: feat

# 步骤 2: 选择范围
? 选择一个 scope（可选）： (Use arrow keys)
❯ animation
  grid
  box-shadow
  theme
  shared
  docs
  config
  deps

# 用户选择: theme

# 步骤 3: 填写描述
? 填写简短精炼的变更描述：
添加暗色模式切换功能

# 步骤 4: 确认
? 确认提交？ (Y/n) Y

# 生成的 commit message:
# feat(theme): 添加暗色模式切换功能

# 自动执行:
# git commit -m "feat(theme): 添加暗色模式切换功能"
```

**与 commitlint 的关系**:

`.cz-config.js` 中的 `types[].value` 必须与 `commitlint.config.js` 中的 `type-enum` 保持一致：

```javascript
// .cz-config.js
types: [
  { value: 'feat', ... },
  { value: 'fix', ... },
  // ...
]

// commitlint.config.js
rules: {
  'type-enum': [2, 'always', [
    'feat',
    'fix',
    // ... 必须相同
  ]],
}
```

如果不一致，Commitizen 生成的提交信息可能无法通过 commitlint 验证。

---

## 🔍 完整提交流程实例

### 场景：添加新功能

**步骤 1: 修改代码**

```bash
# 开发新功能
vim shared/theme.js
# 添加了暗色模式切换功能
```

**步骤 2: 添加到暂存区**

```bash
git add shared/theme.js
```

**步骤 3: 使用 Commitizen 提交**

```bash
npm run commit
```

**步骤 4: 交互式选择**

```text
? 选择你要提交的类型： feat ✨
? 选择一个 scope（可选）： theme
? 填写简短精炼的变更描述： 添加暗色模式切换功能
? 确认提交？ Y
```

**步骤 5: 自动执行 git commit**

```bash
# Commitizen 自动执行:
git commit -m "feat(theme): 添加暗色模式切换功能"
```

**步骤 6: pre-commit 钩子触发**

```text
✔ Preparing lint-staged...
✔ Running tasks for staged files...
  ✔ package.json — 1 file
    ✔ *.{html,css,js,json,md} — 1 file
      ✔ prettier --write
  ✔ shared/theme.js — 1 file
    ✔ *.{js,html} — 1 file
      ✔ eslint --fix
✔ Applying modifications from tasks...
✔ Cleaning up temporary files...
```

**步骤 7: commit-msg 钩子触发**

```text
# commitlint 验证提交信息
✔ feat(theme): 添加暗色模式切换功能
  ✔ type: feat (在允许列表中)
  ✔ scope: theme (可选，格式正确)
  ✔ subject: 添加暗色模式切换功能 (非空)
```

**步骤 8: 提交成功**

```bash
[main abc1234] feat(theme): 添加暗色模式切换功能
 1 file changed, 50 insertions(+)
```

**步骤 9: 推送到远程**

```bash
git push origin main
```

---

## 🚀 立即开始使用

### 推荐方式：使用 Commitizen

```bash
# 1. 修改代码后，添加到暂存区
git add .

# 2. 使用交互式提交（强烈推荐！）
npm run commit

# 3. 按照提示操作：
#    - 选择提交类型（feat/fix/docs...）
#    - 选择影响范围（可选）
#    - 填写简短描述
#    - 确认提交

# 4. 推送到远程
git push
```

### 传统方式：手动提交

```bash
git add .
git commit -m "feat: 添加新功能"
git push
```

## 🎯 核心功能

### 1. 自动代码检查 ✅

提交前自动运行：

- Prettier 格式化
- ESLint 检查和修复
- Stylelint 检查和修复

### 2. 提交信息验证 ✅

确保提交信息符合规范：

- 验证提交类型
- 验证格式
- 拒绝不规范的提交

### 3. 交互式提交 ✨

通过友好的界面生成规范的提交信息：

- 中文提示
- Emoji 图标
- 不会出错
- 自动格式化

## 📝 提交类型速查

| 类型     | Emoji | 说明     | 示例                     |
| -------- | ----- | -------- | ------------------------ |
| feat     | ✨    | 新功能   | `feat: 添加暗色模式`     |
| fix      | 🐛    | 修复 Bug | `fix: 修复布局问题`      |
| docs     | 📝    | 文档更新 | `docs: 更新 README`      |
| style    | 💄    | 代码格式 | `style: 格式化代码`      |
| refactor | ♻️    | 重构     | `refactor: 重构主题系统` |
| perf     | ⚡️   | 性能优化 | `perf: 优化动画`         |
| test     | ✅    | 测试     | `test: 添加测试`         |
| build    | 📦️   | 构建系统 | `build: 更新配置`        |
| ci       | 🎡    | CI 配置  | `ci: 添加 Actions`       |
| chore    | 🔧    | 其他变动 | `chore: 更新依赖`        |

## 🚫 常见问题与解决方案

### Q1: 提交被 pre-commit 钩子拒绝

**解决方案**:

```bash
# 1. 查看具体错误
npm run lint:js

# 2. 手动修复代码中的错误
# 3. 重新添加和提交
git add .
git commit -m "feat: 添加新功能"
```

### Q2: 提交被 commit-msg 钩子拒绝

**解决方案**:

```bash
# 使用正确的格式
git commit -m "feat: 添加新功能"

# 或使用 Commitizen（推荐）
npm run commit
```

### Q3: Commitizen 不显示中文提示

**解决方案**:

```bash
# 1. 检查 package.json 配置
cat package.json | grep -A 5 "commitizen"

# 2. 检查 .cz-config.cjs 是否存在
ls -la .cz-config.cjs

# 3. 重新安装依赖
npm install
```

### Q4: 如何跳过钩子（紧急情况）

```bash
# 跳过所有钩子
git commit --no-verify -m "fix: 紧急修复"

# 或使用简写
git commit -n -m "fix: 紧急修复"
```

**警告**:

- ⚠️ 不推荐经常使用
- ⚠️ 会跳过代码检查，可能引入问题
- ⚠️ 仅在紧急情况下使用

## 🎓 最佳实践

### 1. 提交前自检

```bash
# 在提交前运行完整检查
npm run lint:fix

# 查看将要提交的内容
git diff --staged

# 使用 Commitizen 提交
npm run commit
```

### 2. 提交信息编写规范

**好的提交信息**:

```bash
feat(animation): 添加 3D 变换演示页面

- 实现 rotateX/Y/Z 演示
- 添加 perspective 控制
- 优化移动端体验
```

**不好的提交信息**:

```bash
update  # 太简单，不知道更新了什么
修复    # 缺少 type，不知道修复了什么
feat: fix bug  # type 和内容不匹配
```

### 3. 小步提交

```bash
# ✅ 好的做法：每个功能单独提交
git add shared/theme.js
git commit -m "feat(theme): 添加暗色模式切换"

git add shared/theme.css
git commit -m "style(theme): 优化暗色模式样式"

# ❌ 不好的做法：一次提交太多内容
git add .
git commit -m "feat: 添加很多功能"
```

### 4. 使用 scope 明确范围

```bash
# ✅ 明确的 scope
feat(animation): 添加 3D 变换演示
fix(theme): 修复暗色模式按钮颜色
docs(readme): 更新安装说明

# ❌ 没有 scope（可以，但不够明确）
feat: 添加演示
fix: 修复问题
```

## 📊 项目统计

### 工具集成

- ✅ 8 个工具集成（Husky, lint-staged, commitlint, Commitizen, cz-customizable, Prettier, ESLint, Stylelint）
- ✅ 2 个 Git 钩子（pre-commit, commit-msg）
- ✅ 5 个配置文件（package.json, .husky/\*, commitlint.config.js, .cz-config.cjs）

### 提交规范

- ✅ 11 种提交类型（feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert）
- ✅ 8 个预设 scope（animation, grid, box-shadow, theme, shared, docs, config, deps）
- ✅ 中文提示支持
- ✅ Emoji 图标支持

### 代码质量

- ✅ 自动格式化（Prettier）
- ✅ JavaScript 检查（ESLint）
- ✅ CSS 检查（Stylelint）
- ✅ 只检查暂存文件（lint-staged）

## 🎯 快速命令参考

```bash
# 开发
npm run dev              # 启动开发服务器
npm run build            # 构建项目

# 代码质量
npm run format           # 格式化所有代码
npm run lint             # 检查所有代码
npm run lint:fix         # 修复所有问题

# 提交代码
npm run commit           # 交互式提交（推荐）
git commit -m "..."      # 手动提交

# 验证配置
echo "feat: test" | npx commitlint  # 测试 commitlint
npx lint-staged --help              # 查看 lint-staged
```

## 🎉 总结

### 你现在拥有的能力

1. **自动化代码检查**
   - 提交前自动格式化代码
   - 自动修复常见问题
   - 只检查修改的文件，速度快

2. **规范化提交信息**
   - 交互式引导，不会出错
   - 中文提示，易于理解
   - 自动验证，确保规范

3. **团队协作友好**
   - 统一的代码风格
   - 清晰的提交历史
   - 便于代码审查

4. **可维护性强**
   - 配置文件清晰
   - 易于自定义
   - 文档完善

### 工作流程总结

```text
修改代码 → git add . → npm run commit →
自动格式化 → 自动检查 → 验证提交信息 →
提交成功 → git push
```

### 核心优势

- ✅ **不需要记格式** - 交互式引导
- ✅ **不会出错** - 自动验证
- ✅ **提高效率** - 自动化处理
- ✅ **统一规范** - 团队一致
- ✅ **易于维护** - 配置清晰

## 🔗 相关资源

- [Commitizen 官方文档](https://github.com/commitizen/cz-cli)
- [cz-customizable 文档](https://github.com/leoforfree/cz-customizable)
- [Husky 官方文档](https://typicode.github.io/husky/)
- [lint-staged 文档](https://github.com/okonet/lint-staged)
- [commitlint 文档](https://commitlint.js.org/)
- [约定式提交规范](https://www.conventionalcommits.org/zh-hans/)

## 🚀 开始使用

现在就用 `npm run commit` 代替 `git commit`，享受规范化、自动化的 Git 工作流吧！

```bash
# 1. 修改代码
vim your-file.js

# 2. 添加到暂存区
git add .

# 3. 使用 Commitizen 提交
npm run commit

# 4. 按照提示操作
# 5. 推送到远程
git push
```

---

**配置完成时间**：2025-11-14
**配置状态**：✅ 已完成并可用
**推荐使用**：`npm run commit` 提交代码
**即刻可用**：无需额外配置

🚀 **Happy Coding!**
